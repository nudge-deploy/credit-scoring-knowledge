services:
  api:
    build: .
    image: umkm-kredit-api:latest
    container_name: umkm-kredit-api
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - PORT=8000
